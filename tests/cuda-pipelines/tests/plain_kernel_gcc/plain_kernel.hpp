#ifndef NOARR_CUDA_PIPELINES_TESTS_PLAIN_KERNEL_HPP
#define NOARR_CUDA_PIPELINES_TESTS_PLAIN_KERNEL_HPP

/**
 * A plain kernel that fills a 1024 long array with
 * values from 1 to 1024 and returns the sum
 */
template<typename Item>
Item run_plain_kernel();

#endif

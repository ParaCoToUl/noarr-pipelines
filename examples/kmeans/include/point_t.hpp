#ifndef EXAMPLES_KMEANS_POINT_T_HPP
#define EXAMPLES_KMEANS_POINT_T_HPP

/**
 * Represents a 2D point
 * 
 * This struct is a part of the public API of the kmeans algorithm
 * but may not be used internally during computation
 */
struct point_t {
    float x;
    float y;
};

#endif
